1. For this simple API, We can add users along with other fields, this would however return a JSON response in the browser.

2. Use "Postman" to make a GET, POST & PUT request. 

3. Use "npm install" to install dependencies and also use nodemon.

4. For example, I have added one user "This Name" in the "Members.js" file.